

      <h2> Change Your Password</h2>

             <% flash.each do |key, value| %>
              <div class="alert alert-block alert-<%=get_alert_class(key)%>"> 
              <button type="button" class="close" data-dismiss="alert">×</button>
              <%= value %> 
              </div>
            <% end %>

<div class="container">


<br/>
<br/>

      <div class="row">

                 <div class="grey-box col-xs-8 col-md-offset-2">
                     <div class="hero-block-2">
                      <%= simple_form_for(@user, :wrapper => :bootstrap_horizontal,:url => changepassword_path, :html => {:class => "form-horizontal"}) do |f| %>
                      <br/>
                      <%= f.error_notification %>
                          <%= f.input :password, hint: "(minimum 6 characters)", label: "Create a new Password" %>
                          <%= f.input :password_confirmation, label: "Confirm Password" %>
                          </br>
                        
                          <div class="form-actions">
                           
                          <%= f.button :submit, "Update Password", class: "btn btn-primary", disable_with: "updating..." %>
                       
                          </div>
                          <br/>

                      <% end %>
                     </div>

                 </div>
      </div>
</div>

</br></br></br>
