
<div class="jumbotron">
      <h1> Reset Your Password</h1>
  </div>
             <% flash.each do |key, value| %>
              <div class="alert alert-block alert-<%=get_alert_class(key)%>"> 
              <button type="button" class="close" data-dismiss="alert">×</button>
              <%= value %> 
              </div>
            <% end %>

<div class="container">


<br/>
<br/>

      <div class="row">

                 <div class="well col-xs-8 col-md-offset-2">
                     <div class="hero-block-2">
                       <h4><i class="fa fa-lock fa-lg"></i>&nbsp;&nbsp;<span>Reset Your Password</span></h4>
                      <%= simple_form_for(@user,:wrapper => :bootstrap_horizontal, :url => password_reset_path(params[:id]), :html => {:class => "form-horizontal"}) do |f| %>
                      <br/>
                      <%= f.error_notification %>
                          <%= f.input :password, hint: "(minimum 6 characters)", label: "Create a new Password" %>
                          <%= f.input :password_confirmation, label: "Confirm Password" %>

                        
                          <div class="form-actions">
                           
                          <%= f.button :submit, "Update Password", class: "btn btn-primary", disable_with: "updating..." %>
                       
                          </div>

                      <% end %>
                      </div>

                 </div>
      </div>
</div>
